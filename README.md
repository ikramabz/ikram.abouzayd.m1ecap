# ikram.abouzayd.m1ecap

Ce package contient un jeu de donnees OCDE et des fonctions d'analyse associees.

Le premier commit de ce répertoire a été effectué le 16/04/2026 à 09h43.

## Réponse à l'exercice Git

Suite de commandes `git` à exécuter (hors `git add`) pour obtenir l'historique demandé :

```bash
git checkout master
git commit -m "commit A"
git checkout -b branch1
git commit -m "commit B"
git checkout master
git commit -m "commit C"
git merge branch1 -m "Merge branch 'branch1'"
git commit -m "commit D"
